# T10 - Total Typescript

## Overview

This Thursday T-Class focused on `Total Typescript` and the basics required to write safer, more expressive JavaScript with types.

The class happened on `02 Apr 2026` from `08:00 PM` to `12:00 AM`.

---

## What Is Captured In This Folder

- TypeScript primitive types, arrays, tuples, objects, and function typing
- type inference and safer alternatives like `unknown`
- type aliases, union types, intersection types, and nullable handling
- optional chaining, nullish coalescing, and type assertions
- generics for reusable functions and response shapes
- setup references for Playground/basic intro, local TypeScript, Vite, and Deno

---

## Class Code Highlights

The local class code currently includes:

- `class/src/1-datatype.ts` for basic types, tuples, objects, and inference
- `class/src/2-advanceTypes.ts` for aliases, unions, intersections, and null handling
- `class/src/3-generics.ts` for generic functions, types, and interfaces
- `setup/` examples for intro, local TypeScript, Vite, and Deno setup styles

Check `links.md` for the Excalidraw board and local code references.

---

## Current Status

- class attended live
- class topic is confirmed
- visual board link is added
- local class code is available
- detailed personal notes can still be expanded later
